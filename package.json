{
  "name": "run-in-playwright",
  "version": "1.0.0",
  "description": "browserify a function to run in browser using playwright. The return value and errors are transferred back to node",
  "main": "run-in-playwright.js",
  "scripts": {
    "test": "standard && node test.js | tap-spec"
  },
  "keywords": [
    "run",
    "in",
    "with",
    "browser",
    "isolated",
    "test",
    "playwright",
    "browserify"
  ],
  "author": "KayleePop",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.5.1",
    "playwright": "^0.12.1",
    "tempy": "^0.5.0"
  },
  "devDependencies": {
    "muggle-assert": "^1.1.3",
    "muggle-test": "^1.0.0",
    "standard": "^14.3.3",
    "tap-spec": "^5.0.0"
  }
}
